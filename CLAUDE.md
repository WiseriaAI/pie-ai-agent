# Chrome AI Agent

BYOK (Bring Your Own Key) Chrome Extension — 用户插入自己的 API key 获得 AI 浏览器能力。

## Tech Stack

- Chrome Extension Manifest V3
- React 19 + TypeScript 6
- TailwindCSS v4 (Vite plugin, no config file)
- Vite 8 + @crxjs/vite-plugin 2.4
- pnpm

## Project Structure

- `src/background/` — Service Worker: message routing, port streaming, agent loop dispatch, keep-alive
- `src/content/` — Content Script: placeholder (DOM ops use executeScript injection instead)
- `src/sidepanel/` — Sidebar UI (React): Chat (with Agent UI), Settings (with Skills), tab navigation
- `src/sidepanel/components/` — Chat.tsx, Settings.tsx, AgentStepBubble/AgentConfirmCard/AgentSummary, SkillsList
- `src/lib/model-router/` — Unified LLM interface with tool calling support (AgentMessage IR)
- `src/lib/model-router/providers/` — Anthropic (native tool_use), OpenAI-compatible (function_calling)
- `src/lib/model-router/providers/registry.ts` — Provider metadata registry (supportsTools field)
- `src/lib/dom-actions/` — Self-contained DOM action functions injected via executeScript
- `src/lib/agent/` — ReAct loop, tool registry, risk classifier, prompt builder, sliding window
- `src/lib/agent/tools/keyboard.ts` — Phase 2.5 CDP keyboard tools (dispatch_keyboard_input, press_key)
- `src/lib/agent/tools/skill-meta.ts` — Phase 2.6 skill CRUD meta tools (create_skill / update_skill / delete_skill / list_skills) + previewMetaSkillCall
- `src/lib/agent/tools/tabs.ts` — Phase 3 cross-tab tools (list_tabs, close_tabs, activate_tab, group_tabs, ungroup_tabs, move_tabs, get_tab_content) + extractPageContentHardened (light strip for credential fields)
- `src/lib/agent/untrusted-wrappers.ts` — shared escapeUntrustedWrappers helper covering all <untrusted_*> wrapper sites (Phase 3 P3-O)
- `src/lib/agent/tool-names.ts` — Pure name registry shared by SW + sidepanel (avoids dragging agent runtime into panel bundle) + M3-U4 read/write tool classification (`TOOL_CLASSES` / `getToolClass`) consumed by R7 cross-session lock in loop.ts dispatch; build-time exhaustive check throws if any KNOWN_*_TOOL_NAMES entry is missing a class
- `src/lib/skills/` — Skill framework: types, storage (incl. generateSkillId / getSkillStorageBytes / markSkillFirstRun), builtin, resolveSkillToTools
- `src/lib/crypto.ts` — AES-GCM encryption for API key storage
- `src/lib/storage.ts` — Provider config CRUD (encrypted keys in chrome.storage.local)
- `src/lib/keyboard-simulation.ts` — Phase 2.5 toggle storage helper
- `src/background/cdp-session.ts` — Phase 2.5 CDP lifecycle manager (attach/detach, owner-token guard, generationId); M3-U3 owner-token shape `{sessionId, tabId}` + per-tabId attach/detach mutex `queueTabOp` (closes ADV-9 attach handover race)
- `src/lib/sessions/pinned-tab-registry.ts` — M3-U4 cross-session pinned-tab registry (`getActivePinnedTabs` / `getCrossSessionPinnedTabIds` derived from `session_index`); used by SW dispatch to populate `ctx.refreshCrossSessionPinnedTabIds` so the R7 lock in loop dispatch can refuse write tools targeting a tab another session pins
- `src/types/` — Shared type definitions and message types (chat + agent protocols)

## Supported Providers

Anthropic (native API), OpenAI, OpenRouter, MiniMax, ZhiPu (智谱), Bailian (百炼).
All OpenAI-compatible providers share one streaming implementation via registry.

## Commands

- `pnpm dev` — Start dev server with HMR
- `pnpm build` — Build for production
- `pnpm preview` — Preview production build

## Development

1. `pnpm dev` starts the Vite dev server
2. Go to `chrome://extensions`, enable Developer mode
3. Load unpacked from `dist/` directory
4. Click extension icon to open side panel

## Architecture Notes

- API keys encrypted with Web Crypto API (AES-GCM) in chrome.storage.local, encryption key also in chrome.storage.local
- DOM access via `<all_urls>` host_permission + `chrome.scripting.executeScript` (activeTab insufficient for Side Panel常驻场景)
- Streaming via `chrome.runtime.connect()` port, not sendMessage (supports continuous push)
- Keep-alive pattern: `chrome.runtime.getPlatformInfo()` every 25s during active port connections
- SSE parser handles both `\n` and `\r\n` line endings
- Provider registry pattern: new providers only need a registry entry + host_permission + supportsTools flag
- All injected functions (extractPageContent, snapshotInteractiveElements, click/type/scroll/select) must be self-contained (no closures, args via executeScript)
- ChatMessage stays string-only (Phase 1 wire); AgentMessage IR (string | ContentBlock[]) is SW-internal only
- Agent Loop: tabId+origin pinning at task start, every-round origin check (security)
- Risk classifier: default low + structural escalation (submit buttons, sensitive fields, keyword regex); CDP keyboard tools always high
- Prompt injection defense: page snapshots in user role wrapped in <untrusted_page_content>, never in system role
- Phase 2.5 CDP path: per-task lazy attach via cdp-session.ts; per-CDP-call origin & active-tab re-check; owner-token guard prevents multi-Side-Panel collateral detach; idempotent detach across 5 paths (explicit, abort signal, onDetach, kill-switch, finally); args.text redacted in agent-step but raw in confirm-request (informed approval requires content visibility)
- Phase 2.6 Skill autonomous CRUD invariants: meta tool path enforces 8 capability-grant guards — update_skill rejects builtIn=true (P0-A); parameters JSON Schema strings ≤ 2 KB total share the trust boundary with promptTemplate (P0-B); update_skill taints author='agent' + clears firstRunConfirmedAt so any modification re-triggers R10 first-run confirm regardless of original author (P0-C); promptTemplate ≤ 8 KB AND AgentConfirmCard renders the SW-pre-computed effective merged skill (no 2000-char cap, "(unchanged)" tags retained fields — closes adv-1) (P0-D); create_skill schema additionalProperties:false + handler strips args.id, ids prefixed `skill_agent_`/`skill_user_` to prevent built-in tool name collision (P1-E); allowedTools required non-null array (P1-F); allowedTools names validated against currently-registered tool set, EXCLUDING meta tool names so skills cannot orchestrate further skill CRUD (P1-G + adv residual #1); 1 MB skill_* storage budget (P1-H). R10 first-run gate uses per-iteration skillDefByName cache that is INVALIDATED after any successful meta-tool dispatch (adv-2). Loop layer enforces skill scope (R2) + R3 anti-nest. SkillsList UI is parity for manual create/edit (same caps + name validation).
- Phase 3 cross-tab invariants (P3-A...V): per-call cross-origin args introspection in risk.ts via `hasCrossOriginTab(args, ctx)` (P3-A); read tools also gated by high-risk confirm (P3-B); list_tabs output wrapped in `<untrusted_tab_metadata>` (P3-C); multi-tab confirm wire shape (`tabTargets`) + origin summary row above tab list so K-1 informed-approval survives truncation (P3-E + D-2); manifest tabGroups permission (P3-F); title sanitize: `\n\r\v\f`→space + control-char strip + `escapeUntrustedWrappers` (P3-G + SEC-1 groupName); confirm-time origin re-verify uses `ctx.confirmedTabTargets`, not `pinnedOrigin` (K-8); partial completion observation `ok/skipped/errors` (P3-H); list_tabs cap 50 (P3-I); close_tabs deny pinned tab (P3-J / K-9); incognito deliberately omitted from manifest (P3-K); ≥3-reject task termination (P3-L / K-10 reject-side); activate_tab does not re-pin (P3-M); discarded tab rejects (P3-N); shared `escapeUntrustedWrappers` helper closes ADV-1 `renderTemplate` wrapper-escape gap at `src/lib/skills/index.ts:91` (P3-O); list_tabs scope=allWindows → high (P3-T / SEC-3); get_tab_content SW pre-fetches content + ships preview to confirm card before approval, handler reuses pre-fetched cache (P3-U / SEC-2 / R12); confirm card a11y baseline `role=dialog` + `aria-labelledby` + `OriginSummaryRow` `role=status` + per-row `aria-label` containing cross-origin text (P3-V / R13). G-1 acceptance gate: introducing any low-risk cross-tab tool requires SkillDefinition.allowedTools schema upgrade to (name, scope) tuple FIRST — locked by build-time check in `risk.ts` (every `TAB_TOOL_NAMES` entry must appear in `ALWAYS_HIGH_TAB_TOOLS` or `ARGS_CONDITIONAL_TAB_TOOLS` or the module throws). G-2 gate: cross-window `move_tabs` deferred until confirm card carries source/target window context. P3-Q (BYOK trust boundary acceptance for tab metadata exposure to provider) is documented in solutions, not enforced at runtime.

## Progress

- **Phase 1 (基础对话) — COMPLETED**: Chat with page context, streaming, API key management, 6 providers
- **Phase 0 (元素定位验证) — COMPLETED**: DOM traversal validated, region filtering works, `<all_urls>` permission needed
- **Phase 2 (Agent 能力) — COMPLETED**: ReAct Agent Loop with tool calling, DOM operations, risk-based confirmation, basic Skill framework
- **Phase 2.5 (CDP 键盘模拟) — COMPLETED**: `chrome.debugger` + `Input.insertText` / `Input.dispatchKeyEvent` 支持飞书 Docs 等 canvas 编辑器；二态 Settings 开关，5 路径汇聚的 idempotent detach，per-CDP-call origin re-check，owner-token + generationId 防多 Side Panel 串味，redaction 二分通道（confirm 显示原文 / agent-step redact）
- **Phase 2.6 (Skill 自主 CRUD) — COMPLETED**: SkillDefinition 加 author/createdAt/allowedTools/firstRunConfirmedAt；4 个 meta tools (create/update/delete/list_skill) 注册到 BUILT_IN_TOOLS 并内置 8 个 capability-grant invariant；ReAct loop 强制 skill 作用域白名单 (R2) + 禁嵌套 (R3)；agent 创建/修改的 skill 首次执行二次 confirm (R10)；AgentConfirmCard 对 meta tool 跳过 2000-char cap 并渲染 SW pre-computed effective merged skill (P0-D + adv-1)；Settings SkillsList 升级为可手动 CRUD + author 视觉区分 + 1 MB 配额条
- **Phase 3 (标签管理) — COMPLETED**: 7 cross-tab tools (list_tabs, get_tab_content, close_tabs, activate_tab, group_tabs, ungroup_tabs, move_tabs) + 3 内置 skill (auto_group_tabs / close_duplicate_tabs / close_inactive_tabs); per-call cross-origin risk introspection (P3-A); tabTargets multi-tab confirm wire shape with origin summary row + a11y baseline (P3-E / V); SW pre-fetch + content preview for get_tab_content (P3-U); shared escapeUntrustedWrappers helper closes ADV-1 renderTemplate wrapper-escape gap (P3-O); 19 P3 invariants enforced (P3-A to P3-V minus folded P3-D/Q/R); G-1 acceptance gate locked by build-time check in risk.ts; G-2 acceptance gate for cross-window move_tabs. Manifest version bumped 0.3 → 0.4 (Chrome will request re-permission for new tabGroups).
- **Phase 4 / M1 (Session as First-Class Persistent Layer — single-session checkpoint & resume) — COMPLETED**: 三 storage key 拆分 (`session_index` 单 key + `session_${id}_meta` panel 域 + `session_${id}_agent` SW 域) by D2; `writeAtomic` helper 集中 D9 单调用原子性; ID 走 `crypto.randomUUID()` 无前缀 (PRD-3 fix → M2 无 migration); `useSession` hook 在 App 层 mount-即连 + 持久 listener 跨 sub-view (5 个 done 边界写 storage, mid-stream chunk/step/confirm 不写); `buildSessionAgentSnapshot` 用 `structuredClone` 深 clone 防 mutate-after-snapshot (D4); `buildSessionAgentTombstone` 在 `emitDone` + pure-text-reply 写空 in-flight 标记防 cold-start 误判 (commit f4bb1d0 用户报告 P0); R28 v2 重读 — storage 持 raw `agentMessages`, panel 显走独立 `redactArgsForPanel` path (LLM resume 需要 raw context); `SessionConfirmRequestMessage` distinct variant + `panel-mounted` wire identity message + 双源 invariant (storage record + SW pendingConfirmations 都活才重推 R4); `detectAndMarkPaused` 走 SW 顶层主路径 (MV3 `onStartup` 在 idle wake-up 不 fire), 30s `recovery_guard` 独立 key 防重, 步骤顺序 markFailed→scrub→markPaused→guard 严格不可换序; R11 drift card 单 'Discard' button (BYOK informed-spending: 不 silent auto-resume); resume 路径 `isResumedFirstIteration` 跳过第一轮 observation merge 防 double-observation 陷阱; 76 测试 (storage 39 / useSession 20 / loop 9 / recovery 8) + happy-dom + @testing-library/react infra 首次引入. 详细 invariants 见 `docs/solutions/2026-05-02-session-as-first-class-persistent-layer-m1.md`.
- **Phase 4 / M2-U1 (multi-session schema + state machine + skillExecutionScopeStack) — COMPLETED via PR #9**: `state-machine.ts` `canTransition` + `ALL_TRANSITIONS` (4 状态 `active|paused|failed|archived`, 同状态自转 idempotent); `migration.ts` idempotent `default-id` 残留清理, SW startup `recoveryReady` promise 串行化 `migration → detectAndMarkPaused`; `lastAccessedAt` 三 trigger (chat-start fire-and-forget bump + step counter `% 5 === 0` 节流防 meta 写抖动 + tombstone 命中); `loop.ts:511` `currentSkillScope: SkillScope|null` 升级为 `skillExecutionScopeStack: SkillScope[]` (function-local, M3-U1 多 session 天然隔离), snapshot 持久化 + resume `AgentLoopContext.resumedSkillScopeStack?` 还原, R2 白名单 + R3 anti-nest 语义不变. 76 → 106 测试.
- **Phase 4 / M2-U3 + M2-U4 (LLM async 标题 + R29 untrusted_user_message wrapper + LRU archive + 30d hard delete + soft delete + storage indicator) — COMPLETED**: M2-U3 加 4th wrapper tag `untrusted_user_message` (P3-O 双 list lock-step 自动断言落地为 vitest fs-read 测试 — `untrusted-wrappers.test.ts` 检查 `UNTRUSTED_WRAPPER_TAGS` 数组每条都在 `dom-actions/snapshot.ts` inline regex 出现); 共享 `src/lib/sessions/title.ts` `deriveTitleFromMessages` (panel + SW 双侧用 — M2-U2 fallback `useSession.ts` 抽走); `title-generator.ts` `generateTitle(firstMsg, callChat)` DI 接口 + 模板用 escapeUntrustedWrappers 包 user 消息 + LLM 输出再 escape + emoji strip (扩展 regex 至 `[\u{2300}-\u{27BF}\u{1F300}-\u{1FAFF}]` 含 Dingbats) + 截断 30 字; `maybeUpgradeFallbackTitle` race-guard via 等值比较 — **slash-skill bug 修正 (ce:review 暴露)**: SW 不再用 `deriveTitleFromMessages(messages)` 算 sentinel (那会用 expandedForLLM 导致 panel 写的 raw fallback `/myskill` ≠ SW 算的 expanded `Use foo to do Y`，race-guard 永久 fail)，改成 `await getSessionMeta(sessionId).title` 读 panel-written sentinel; handleChatStream 在 `messages.length===1 && role==='user'` fire-and-forget 触发. M2-U4 `lifecycle.ts` 5 函数 `archiveSession` / `unarchiveSession` / `softDeleteSession` / `hardDeleteSession` / `hardDeleteExpired` / `checkAndArchiveLRU`; **archived bundle schema 简化** (ce:review 暴露 archive-bytes-grow 真 bug — 顶层 `archivedAt` 与 `meta.archivedAt` 冗余使小 session bundle 反增 +12 字节，移除顶层 + hardDeleteExpired 改读 `payload.meta.archivedAt`); archive 走 `writeAtomic` 单调用 (set archivedKey + remove meta + remove agent + update index) 原子 + 自然绕过 `setSessionAgent` quota guard 不递归 (`writeAtomic` is the canonical path, `setSessionAgent` 不在 archive 路径); `setSessionAgent` 加 pre-write guard `getBytesInUse(null) >= 8MB` 触发 `checkAndArchiveLRU` (lazy import 破循环) MAX 5 archive/call cap; `hardDeleteExpired` opportunistic on sidepanel mount (App.tsx mount useEffect 加 `.catch` 防 unhandledrejection); **malformed archive 防御**: `payload?.meta?.archivedAt ?? Number.MAX_SAFE_INTEGER` 让 corrupt 条目存活、可在 'Show Archived' 中手动 Delete Forever 而不是被静默立即清; SessionDrawer.tsx 加 StorageIndicator (>7.5MB 红色高亮) + Show Archived 折叠 section + ArchivedRow (Restore/Delete forever) + SessionRowWithDelete hover-reveal Archive 按钮 (paused row 隐藏避开 Resume); **panel-side persistMessages 加 archived-status guard** 防 LRU 期间 streaming snapshot 把 meta 写回让 session 同时存在两个 bucket; **R28 v2 archive-time redaction policy**: storage 持 raw agentMessages at-rest 与 active session 一致 (panel-render 才走 redactArgsForPanel —— archive 不再过 redact 过滤). 143 → 172 测试 (untrusted-wrappers 6 / title-generator 11 / lifecycle 12). M3 (真 per-session sandbox: per-session port + per-session pinned tab/origin) 在后续 PR.
- **Phase 4 / M3 (真 per-session sandbox) — COMPLETED**: 5 ship-units delivering multi-session sandbox; trace doc `docs/solutions/2026-05-03-multi-session-invariant-trace.md` is the R24/R25/R26 acceptance gate. **M3-U1 per-session port routing** — port name `chat-stream-${sessionId}`; SW parses sessionId from port.name + `verifyPortSession` drops every panel→SW message whose sessionId disagrees with the port-bound id (silent drop now also emits chat-error so panel resets streaming=true; ce:review fix); panel `useSession.connectPortFor` swaps the port (disconnect old + connect new) on `setActive` / `createAndActivate`. **M3-U2 per-session pinned tab/origin** — `captureActivePinned` (with restricted-URL prefix filter incl. blob:/chrome:// to keep panel and loop in sync) at session creation/activation persists `pinnedTabId/pinnedOrigin` into SessionMeta; SW dispatcher reads meta and constructs a single `ctx.pinned: {tabId, origin}` (both-or-neither at the type level — collapsed from two independent optionals after kt-1 review); loop trusts ctx and skips active-tab anchor; legacy fallback preserved; bootstrap + setActive backfill missing pins for M1/M2 sessions, capture-once-never-overwrite. **M3-U3 CDP `ownerToken = {sessionId, tabId}`** — multi-Side-Panel collateral conflict message names the offending session UUID; per-tabId `queueTabOp` mutex serializes acquire/detach so ADV-9 attach handover race (session A's `chromeDetach` overlapping with session B's `chromeAttach`) is closed; `prev.then(op, op)` keeps the chain alive across failed prior ops. **M3-U4 read/write tool classification + R7 cross-session lock** — `TOOL_CLASSES` registry in `tool-names.ts` (build-time exhaustive check derived from `KNOWN_*_TOOL_NAMES` spread — no hand-maintained duplicate list after MAINT-M3-4 review fix); `collectCrossSessionConflicts` pure helper called per tool dispatch in loop.ts; **TOCTOU fix**: `ctx.refreshCrossSessionPinnedTabIds()` re-reads `session_index` per dispatch instead of frozen-snapshot at chat-start so sessions created mid-loop are observed by the lock; **shared-pin deadlock fix** (adversarial review): non-tab write tools no longer fold pinnedTabId into the conflict check (the fold was symmetric-deadlocking two sessions that share a pin while adding no offsetting safety — per-iteration origin re-check already protects intent); R7 observation refined to drop the misleading "or wait for it to finish" suggestion. **M3-U5** trace doc + per-session locality regression tests in `loop.test.ts` (`buildSessionAgentSnapshot` independence + `collectCrossSessionConflicts` per-dispatch independence). 172 → 215 tests (+5 cdp-session: ownerToken / sibling-tab isolation / queueTabOp tail tolerance / ADV-9 race with chrome-layer detach simulation; +6 pinned-tab-registry; +8 TOOL_CLASSES; +10 collectCrossSessionConflicts variants; +2 useSession port swap; +5 useSession pin capture/blob/setActive backfill/no-overwrite; +3 M3-U5 multi-session locality; +4 misc). 详细 invariants + verdicts 见 trace doc.
- **Phase 4 / M2-U2 (multi-session sidepanel UI + ce:review pass-2 fix sweep + manual-acceptance fix sweep) — COMPLETED via PR #10**: 4 新 component (`TopBarListButton` 24×24 ≡ + 7px corner pending dot + 1.5px bg `#080D10` island + ice silver border on isOpen / `TopBarNewSessionButton` 24×24 + button / `SessionDrawer` 296px overlay + backdrop rgba(8,13,16,0.72) + 底部 STORAGE bar / `SessionRow` 5 status SVG icons inline 按 paper artboards 07/08); 顶栏布局 `[≡●] [+] {sessionTitle 纯文本} ─── [☼]` (列表按钮最左 / 新建按钮居中 / title 纯文本非 click target — 设计 iteration 后定); `SessionDrawer` overlay drawer + ESC + focus trap (FOCUSABLE_SELECTORS 含 `[role='listitem']`); SEC-PLAN-009 flood-limit guard `getPendingConfirmCount > 5` → auto-deny + `session-toast`; R23 archived active session → 自动 createAndActivate 后 dispatch slash skill; **wire-format upgrade**: 8 个 `PortMessageToPanel` variant (`chat-chunk` / `chat-done` / `chat-error` / `agent-step` / `agent-confirm-request` / `agent-done-task` / `session-toast` / `session-confirm-request`) 全部加 `sessionId: string` 必填字段 + `useSession.handlePortMessage` filter (防止旧 session 的 confirm 在新 session UI 渲染让用户在错误 context approve high-risk action against 旧 pinned tab — K-1 informed-approval); `getPendingConfirmCount` 仅 count `pendingConfirm.kind === 'agent-tool'` (drift-card 不入 flood 计数, 防止永久 flood-DoS cascade); `createAndActivate` + `resumeTask` 加 streaming guard (P0-1 + P0-2); `SessionDrawer.onClose` 在 App.tsx `useCallback` 包装 (防 focus trap effect 在 parent re-render 时 re-run clobber focus); `SessionRow` `tabIndex={0}` + Enter/Space onKeyDown + Show Archived as `<button aria-disabled>`; AgentConfirmResponseMessage + SW pendingConfirmationsBySession Map 双重校验 sessionId (defense-in-depth). **Manual-acceptance post-fixes**: (Bug-A/B `96bfdff`) `streamingRef` 同步设置 + `messagesRef` lock-step + metaKey listener strict-prefix 防 SW `updateLastAccessed` 在 chat-start fire-and-forget round-trip 把 stale messages 写回时抹掉 panel 的 user msg / assistant text; (Bug-C `96bfdff`) `deriveTitleFromMessages` first-user 40-char fallback 写入 `meta.title` (M2-U3 LLM 标题前的桥); (Bug-PINNED `9d8d728`) Chat.tsx pinnedOrigin 增加 `chrome.tabs.onActivated` + `chrome.windows.onFocusChanged` 监听 (旧版只监听 `onUpdated` url-change 错过 active-tab 切换), streaming-aware (mid-stream 锁住 task 启动 origin); (Bug-E `6527ce8`) per-port `inFlightSessionIds: Set<string>` (chat-start + resume-task add) + `transitionPortInFlightSessionsToPaused` helper, `port.onDisconnect` 调 helper 仅扫此 port 的 session 走 detectAndMarkPaused 同款 step-1/step-2 (镜像 Phase 2.5 CDP owner-token 多 sidepanel 隔离不变量) — 修 panel 关闭后 in-flight session status 不转 paused 导致重开看不到 RESUME TASK 按钮; (Bug-D `6527ce8`, advisor surface) `pendingConfirmations` resolver 升级为结构化 `(result: {approved, reason?: 'user-reject'|'aborted'}) => void`, abort drain (signal listener + onDisconnect) 用 `reason='aborted'`, `sendConfirmRequest` 返回类型加 `'aborted'`, loop K-10 判断从 `!== 'flood-limit'` blacklist 改为 `=== 'user-reject'` whitelist (任何未来 reason 默认不计入) — 修关 panel 中断 confirm 错误污染 K-10 fatigue counter 让下次 resume 触发 "User repeatedly rejected" 自动终止. 106 → 143 测试 (drawer 17 / SEC-PLAN-009 boundary 8 / loop K-10 whitelist 5 / panel-disconnect transition 7). M2-U3 (LLM async 标题) + M2-U4 (LRU archive + 30d 硬删) + M3 (真 per-session sandbox) 在后续 PR.
